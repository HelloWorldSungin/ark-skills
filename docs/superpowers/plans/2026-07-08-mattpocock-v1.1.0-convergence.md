# mattpocock v1.1.0 Convergence Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update the ark-skills plugin's references to mattpocock/skills from the retired pre-v1.1.0 names to the current v1.1.0 names, wire `to-tickets`/`research` into the existing conventions, and bump the plugin version — with zero behavior expansion beyond the three approved opportunities.

**Architecture:** Pure documentation/config edits (Markdown + JSON) across 12 files. No source code, no test suite. Each task's "test cycle" is a `git grep` / snippet-run / manual consistency read. Work follows the reviewed spec at `docs/superpowers/specs/2026-07-08-mattpocock-v1.1.0-convergence-design.md`.

**Tech Stack:** Markdown, JSON, `git grep`, `gh` (referenced in docs only), bash (health-check snippets).

## Global Constraints

- **Scope = Minimal label model.** Keep `epic→story→task` + component (`consultant`/`conventions`/`vault`/`onboarding`) + `P1`/`P2`/`P3`. Do NOT adopt `wayfinder:*` labels. Do NOT change `epic→story→task`.
- **Rename map (verbatim):** `to-issues`→`to-tickets`; `to-prd`→`to-spec`; `to-plan`→folded into `to-tickets`; `decision-mapping`→`wayfinder`; `review`→`code-review`; `qa`→retired (no replacement).
- **Classification rule:** `to-issues`/`to-prd`/`to-plan`/`decision-mapping` are unambiguous → replace every occurrence. `review`/`qa` are generic words → change ONLY the single mattpocock-context occurrence (`docs/agents/issue-tracker.md` L31). Never touch `skills/ark-health/SKILL.md` L43 (gstack Check 2 detection tokens) or `skills/ark-consult/SKILL.md` L49 ("review tooling" prose) or `docs/superpowers/specs/2026-04-08-*.md` (historical).
- **Do NOT patch mattpocock's installed skill files** (`~/.claude/skills/**`). This plan only edits files inside this repo.
- **No push, no PR.** Commits are local only.
- **Per-file one-pass rule:** `skills/ark-consult/SKILL.md` and `.omc/drafts/mattpocock-contract.md` receive both rename and semantic edits — do each in a single task/pass (Tasks 3 and 4), never twice.
- **AI-disclaimer discrepancy stays as-is:** both `> *This was generated by AI.*` and `> *This was generated by AI during triage.*` are valid; do not reconcile.

---

## File Structure

| File | Task | Responsibility of the change |
|---|---|---|
| `skills/ark-onboard/SKILL.md` | 1 | rename `/to-issues`→`/to-tickets` (L12) |
| `README.md` | 1 | rename (L45) |
| `AGENTS.md` | 1 | rename (L65) |
| `skills/AGENTS.md` | 1 | rename (L97) |
| `skills/ark-health/SKILL.md` | 2 | harden Check 3 + rename Unlocks (L48–56) |
| `skills/ark-consult/SKILL.md` | 3 | rename + `to-tickets` engine note + entry-point sharpening + wayfinder escape hatch |
| `.omc/drafts/mattpocock-contract.md` | 4 | full rewrite against v1.1.0 skills |
| `.omc/drafts/issue-tracker-ark-additions.md` | 5 | stale-draft rename (L24, L46) |
| `docs/agents/issue-tracker.md` | 6 | rename (L31/L48/L72) + research-landing cross-ref |
| `CLAUDE.md` | 7 | add `Research notes → vault/Research/` config row |
| `skills/vault/SKILL.md` | 7 | name `research` output as an ingest source (§2) |
| `VERSION`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `CHANGELOG.md` | 8 | bump 2.2.1 → 2.3.0 |

---

## Task 1: Simple mechanical renames (4 files)

Four identical `/to-issues` → `/to-tickets` substitutions in prose. No semantic change.

**Files:**
- Modify: `skills/ark-onboard/SKILL.md:12`
- Modify: `README.md:45`
- Modify: `AGENTS.md:65`
- Modify: `skills/AGENTS.md:97`

**Interfaces:**
- Consumes: nothing.
- Produces: nothing (prose only).

- [ ] **Step 1: Edit `skills/ark-onboard/SKILL.md` line 12**

Replace `(`/to-issues`, `/triage`)` with `(`/to-tickets`, `/triage`)`:
```
3. **mattpocock/skills** as the issue machinery (`/to-tickets`, `/triage`) and the consultant's routing destinations (superpowers, gstack, OMC).
```

- [ ] **Step 2: Edit `README.md` line 45**

In the `/ark-consult` paragraph, change `optionally cut via mattpocock `/to-issues`` to `optionally cut via mattpocock `/to-tickets``. Leave the rest of the line unchanged.

- [ ] **Step 3: Edit `AGENTS.md` line 65**

```
- **mattpocock/skills** — issue machinery (`/to-tickets`, `/triage`) and a routing destination for `/ark-consult`.
```

- [ ] **Step 4: Edit `skills/AGENTS.md` line 97**

```
Optional integrations: the `gh` CLI (GitHub Issues task management), the `notebooklm` CLI (synthesized recall), and mattpocock/skills (`/to-tickets`, `/triage`). See the parent `AGENTS.md` for details.
```

- [ ] **Step 5: Verify no retired names remain in these 4 files**

Run: `git grep -nE "to-issues|to-prd|to-plan|decision-mapping" -- skills/ark-onboard/SKILL.md README.md AGENTS.md skills/AGENTS.md`
Expected: no output (exit 1).

- [ ] **Step 6: Commit**

```bash
git add skills/ark-onboard/SKILL.md README.md AGENTS.md skills/AGENTS.md
git commit -m "fix(ark-skills): rename retired /to-issues → /to-tickets in prose refs"
```

---

## Task 2: Harden `/ark-health` Check 3 + rename Unlocks

Check 3 currently greps for `to-issues`/`to-prd` (retired) and passes on `≥3-of-4`, which both false-*fails* today and could false-*pass* if `to-tickets` were the missing skill. Replace with a required-set + soft-count.

**Files:**
- Modify: `skills/ark-health/SKILL.md:48-56`

**Interfaces:**
- Consumes: nothing.
- Produces: the hardened Check 3 that Task 9 / spec verification relies on.

- [ ] **Step 1: Replace the Check 3 detect block (lines 50–53)**

Replace:
```bash
  n=0; for s in to-issues triage to-prd setup-matt-pocock-skills; do [ -d "$HOME/.claude/skills/$s" ] && n=$((n+1)); done
  [ "$n" -ge 3 ] && echo "PASS: $n/4 mattpocock skills present" || echo "FAIL: only $n/4 present"
```
with:
```bash
  req=0; for s in to-tickets setup-matt-pocock-skills; do [ -d "$HOME/.claude/skills/$s" ] && req=$((req+1)); done
  opt=0; for s in to-spec triage implement; do [ -d "$HOME/.claude/skills/$s" ] && opt=$((opt+1)); done
  { [ "$req" -eq 2 ] && [ "$opt" -ge 2 ]; } && echo "PASS: to-tickets+setup present, $opt/3 optional" || echo "FAIL: req=$req/2 opt=$opt/3"
```

- [ ] **Step 2: Update the Pass line (line 54)**

Replace `- Pass: ≥3 of the 4 skill dirs present` with:
```
- Pass: `to-tickets` + `setup-matt-pocock-skills` present, plus ≥2 of `to-spec`/`triage`/`implement`
```

- [ ] **Step 3: Update the Unlocks line (line 56)**

Replace `- Unlocks: `/to-issues` + `/triage` issue machinery and mattpocock as a routing destination` with:
```
- Unlocks: `/to-tickets` + `/triage` issue machinery and mattpocock as a routing destination
```

- [ ] **Step 4: Leave Check 2 (line 43) untouched; fix the stale denominator in the sample output (line 248)**

Confirm `skills/ark-health/SKILL.md:43` still reads `browse`, `qa`, `ship`, `review`, `design-review` (gstack detection — NOT mattpocock skill names). Do not edit it.

The sample-output line 248 reads `!!  mattpocock skills -- only 1/4 present`. The roster is now 5 skills, so update the denominator for formatting hygiene: change `only 1/4 present` to `only 1/5 present`. (The fix hint on line 249 names no retired skill — leave it.)

- [ ] **Step 5: Verify the new Check 3 passes on this machine**

Run:
```bash
req=0; for s in to-tickets setup-matt-pocock-skills; do [ -d "$HOME/.claude/skills/$s" ] && req=$((req+1)); done
opt=0; for s in to-spec triage implement; do [ -d "$HOME/.claude/skills/$s" ] && opt=$((opt+1)); done
{ [ "$req" -eq 2 ] && [ "$opt" -ge 2 ]; } && echo "PASS: to-tickets+setup present, $opt/3 optional" || echo "FAIL: req=$req/2 opt=$opt/3"
```
Expected: `PASS: to-tickets+setup present, 3/3 optional`

- [ ] **Step 6: Verify no retired names remain in ark-health except intended non-mattpocock tokens**

Run: `git grep -nE "to-issues|to-prd|to-plan|decision-mapping" -- skills/ark-health/SKILL.md`
Expected: no output. (The generic `qa`/`review` on line 43 are not in this pattern and correctly remain.)

- [ ] **Step 7: Commit**

```bash
git add skills/ark-health/SKILL.md
git commit -m "fix(ark-health): harden Check 3 for mattpocock v1.1.0 roster (require to-tickets)"
```

---

## Task 3: `/ark-consult` — rename + `to-tickets` engine + wayfinder escape hatch (one pass)

Combines Phase 1a rename and Phase 2 semantic additions for this file per the one-pass rule.

**Files:**
- Modify: `skills/ark-consult/SKILL.md` (L70, L109, L116, L119 rename; child-cutting section semantic additions; handoff/matrix wayfinder note)

**Interfaces:**
- Consumes: the hardened routing (no code interface).
- Produces: the routing/handoff prose that the rewritten contract (Task 4) cross-references.

- [ ] **Step 1: Rename in the known-overlap rule (line 70)**

Change `mattpocock `grill-with-docs` → `to-prd`` to `mattpocock `grill-with-docs` → `to-spec` → `to-tickets``.

- [ ] **Step 2: Rename `/to-issues` → `/to-tickets` at line 109**

Replace line 109 exactly:
```
child-checklist maintenance are also yours: mattpocock's `/to-issues` explicitly
```
with:
```
child-checklist maintenance are also yours: mattpocock's `/to-tickets` explicitly
```

- [ ] **Step 3: Replace the "Delegate to mattpocock" bullet (lines 116–121) — rename + engine note in one block**

Replace this exact block (lines 116–121):
```
- **Delegate to mattpocock `/to-issues`** (enhancement): explicitly invoke the
  slash command with the plan in context. mattpocock skills declare
  `disable-model-invocation: true` — they NEVER auto-trigger, so delegation means
  literally invoking `/to-issues`. It files children in dependency order with
  `ready-for-agent` and wires sub-issue edges, but never edits the epic — you check
  the boxes yourself. The `gh` fallback is always available if you don't delegate.
```
with:
```
- **Delegate to mattpocock `/to-tickets`** (enhancement): explicitly invoke the
  slash command with the plan in context. mattpocock skills declare
  `disable-model-invocation: true` — they NEVER auto-trigger, so delegation means
  literally invoking `/to-tickets`. It files children in dependency order with
  `ready-for-agent` and wires **native blocking edges + sub-issue relationships**,
  but never edits the epic — you check the boxes yourself. **Wide refactors** are
  sequenced as **expand–contract** (add-new-beside-old → migrate call sites in
  blast-radius batches → delete old) rather than forced into one tracer-bullet slice.
  `/ark-consult` still owns the epic + `component` + `P1`/`P2`/`P3` labels. The `gh`
  fallback is always available if you don't delegate.
```

- [ ] **Step 4: Insert the entry-point + wayfinder note as a paragraph between the matrix and the Known-overlap header (lines 59–61)**

Replace this exact block (lines 59–61):
```
| Tracker-driven implementation | mattpocock | `/implement` per issue | Drives TDD + review from a filed issue |

#### Known-overlap rules (pick one, NEVER chain)
```
with:
```
| Tracker-driven implementation | mattpocock | `/implement` per issue | Drives TDD + review from a filed issue |

**mattpocock lane entry point depends on task phase:** a build you can hold in one
session → `/to-spec` → `/to-tickets`; an already-filed issue → `/implement`. Foggy,
multi-session efforts route to the **OMC lane** (`ralplan`/`autopilot`), not
mattpocock — mattpocock's `/wayfinder` remains a manual, off-profile choice under the
current Minimal label model (it applies its own `wayfinder:*` labels that ark-skills
does not adopt).

#### Known-overlap rules (pick one, NEVER chain)
```

- [ ] **Step 5: Verify rename complete and wayfinder framed correctly**

Run: `git grep -nE "to-issues|to-prd|to-plan" -- skills/ark-consult/SKILL.md`
Expected: no output.
Run: `git grep -n "wayfinder" skills/ark-consult/SKILL.md`
Expected: the new note; confirm it says manual/off-profile, NOT auto-routed.

- [ ] **Step 6: Confirm line 49 prose untouched**

Run: `sed -n '49p' skills/ark-consult/SKILL.md`
Expected: still contains "Owns the design-system + review tooling" (generic prose — not renamed).

- [ ] **Step 7: Commit**

```bash
git add skills/ark-consult/SKILL.md
git commit -m "feat(ark-consult): route to /to-tickets engine + wayfinder escape hatch (Minimal)"
```

---

## Task 4: `.omc/drafts/mattpocock-contract.md` — full rewrite

The contract documents skills that no longer exist (`to-issues`, `to-prd`). Rewrite it against the installed v1.1.0 skills. It is tracked and pointed at by `ark-consult` and `issue-tracker.md`, so downstream clones read it.

**Files:**
- Modify (overwrite): `.omc/drafts/mattpocock-contract.md`

**Interfaces:**
- Consumes: the routing prose from Task 3.
- Produces: the delegation contract that `ark-consult` (L108) and `issue-tracker.md` (L72) reference.

- [ ] **Step 1: Replace the entire file with the rewritten contract**

```markdown
# mattpocock skills delegation contract (re-validated 2026-07-08 for v1.1.0)

Read against the actual installed SKILL.md files at
`~/.claude/skills/{to-tickets,to-spec,triage,code-review,setup-matt-pocock-skills}/SKILL.md`
and the `docs/agents/*.md` this repo has. This supersedes the 2026-07-06
contract, which described the pre-v1.1.0 child-cutting and PRD-synthesis skills that
v1.1.0 retired.

## Invocation surface

`/to-tickets`, `/to-spec`, and `/triage` declare `disable-model-invocation: true` in
frontmatter — they never auto-trigger and are reachable only via an explicit slash
command. `/code-review` is the **exception**: its frontmatter does NOT set that flag,
so it is model-invokable (it can auto-trigger and other skills can reach it). For the
three flagged skills, "delegating to mattpocock" means literally invoking the slash
command — there is no ambient routing they do on their own — so `ark-consult` must make
those handoffs explicit invocations.

- **`/to-tickets`** — takes a plan/spec/conversation (in context, or a spec path /
  issue reference passed as an argument) and breaks it into **tracer-bullet vertical
  slices**, each declaring its **blocking edges**, filed after a quiz-the-user loop.
  On a real tracker it uses **native blocking links + sub-issue relationships** and
  publishes in dependency order; **wide refactors** are sequenced as **expand–contract**
  instead of a single slice. Applies `ready-for-agent` by default unless told
  otherwise. (Consolidates the two pre-v1.1.0 child-cutting skills.)
- **`/to-spec`** — turns the current conversation thread into a spec and publishes it.
  Pure synthesis of what's already been discussed. (Successor to the pre-v1.1.0
  PRD-synthesis skill.)
- **`/triage`** — moves an issue/PR through the 5-role state machine
  (`needs-triage`/`needs-info`/`ready-for-agent`/`ready-for-human`/`wontfix`) plus
  the `bug`/`enhancement` category roles. Invoked with natural language. Does
  codebase verification and optional grilling before recommending a state. v1.1.0
  `/triage` also handles external PRs.
- **`/code-review`** — two-axis review of a diff against a fixed point: **Standards**
  (repo coding standards + a fixed Fowler ~12-smell baseline) and **Spec** (does the
  diff implement the originating issue/spec), run as parallel sub-agents and reported
  side by side. (Successor to the retired `review`.) `ark-consult` does NOT own review
  — it belongs to whichever ecosystem the work was handed to.

## What they read

- `docs/agents/issue-tracker.md` — tracker identity (GitHub via `gh`, this repo, no
  PR-triage surface), `gh` conventions.
- `docs/agents/triage-labels.md` — canonical-role → label-string mapping (identity
  here: the 5 roles use this repo's exact label strings).
- `docs/agents/domain.md` — `CONTEXT.md`/`docs/adr/` consumer rules (skills proceed
  silently when neither exists).
- `CONTEXT.md` / `docs/adr/` directly, when they exist.

## What they write

- New issues (`to-tickets`, `to-spec`) via `gh issue create`, with native blocking /
  sub-issue links (`to-tickets`) where the tracker supports them.
- Labels on existing issues (`triage`) via `gh issue edit --add-label/--remove-label`.
- Comments (`triage`, and any narrative comment) via `gh issue comment`.
- Closes (`triage`) via `gh issue close --comment`.
- `.out-of-scope/*.md` files (triage, for rejected-enhancement `wontfix` only).

## What they never do

- **`to-tickets` explicitly states: "Do NOT close or modify any parent issue."**
  Children only reference the parent; they never edit it.
- None of them touch epic/parent issues — `ark-consult` owns epic creation and
  epic-body child-list maintenance (checking off `- [x] #NNN` as children close). That
  is NOT delegated to mattpocock.
- `to-spec` never interviews — it synthesizes an existing thread. If the conversation
  hasn't surfaced enough detail, use `to-tickets`' quiz loop or `/grilling` via
  `triage` instead.

## AI-disclaimer convention — two valid strings, do not reconcile

- **ark-wide** (this repo's `docs/agents/issue-tracker.md`): `> *This was generated by AI.*`
- **mattpocock's `/triage`** (hardcoded in its own SKILL.md, outside this repo's
  control): `> *This was generated by AI during triage.*`

`to-tickets`/`to-spec`/`code-review` specify no disclaimer of their own — they inherit
the ark-wide string. Only `/triage` hardcodes its variant. Document both as valid; do
NOT patch mattpocock's installed skill files.

## Component labels

This repo's four component labels (`consultant`, `conventions`, `vault`, `onboarding`)
are ark-specific and outside mattpocock's vocabulary (which knows only the 5 triage
roles + 2 category roles). `ark-consult` applies component labels itself; delegating an
issue to `/triage` only ever touches triage-state and category labels.

## wayfinder — available but NOT wired (Minimal profile)

v1.1.0 adds `/wayfinder` (foggy multi-session efforts as a `wayfinder:map` issue with
child tickets + native blocking). ark-skills does **not** wire it into `ark-consult`
routing under the current **Minimal label model**, because `wayfinder` hardcodes its
own `wayfinder:map` / `wayfinder:<type>` labels, which the Minimal model does not
adopt. Foggy multi-session work routes to the OMC lane (`ralplan`/`autopilot`); a user
may invoke `/wayfinder` manually as an off-profile choice. Wiring it natively is the
content of a future "Additive" bump.
```

- [ ] **Step 2: Verify no retired names remain**

Run: `git grep -nE "to-issues|to-prd|to-plan|decision-mapping" -- .omc/drafts/mattpocock-contract.md`
Expected: no output.

- [ ] **Step 3: Verify the pointers into this file still resolve**

Run: `git grep -n "mattpocock-contract.md" -- skills/ark-consult/SKILL.md docs/agents/issue-tracker.md`
Expected: both references present (they point at this file, which still exists).

- [ ] **Step 4: Commit**

```bash
git add .omc/drafts/mattpocock-contract.md
git commit -m "docs(contract): rewrite mattpocock delegation contract for v1.1.0 skills"
```

---

## Task 5: `.omc/drafts/issue-tracker-ark-additions.md` — stale-draft rename

Superseded staging draft of `docs/agents/issue-tracker.md`; still carries retired refs at L24 and L46. Sweep the rename to keep the audit grep clean (do not delete — it is not ours to remove and deletion is less reversible).

**Files:**
- Modify: `.omc/drafts/issue-tracker-ark-additions.md:24,46`

**Interfaces:**
- Consumes / Produces: nothing.

- [ ] **Step 1: Edit line 24**

Line 24 begins with the token (the word "mattpocock" is at the end of line 23 — do not include it). Single-line edit on line 24: change `` `/to-issues` / `/triage` output`` to `` `/to-tickets` / `/triage` output``.

- [ ] **Step 2: Edit line 46**

Change `delegates child-cutting to mattpocock `/to-issues` per the` to `delegates child-cutting to mattpocock `/to-tickets` per the`.

- [ ] **Step 3: Verify**

Run: `git grep -nE "to-issues|to-prd|to-plan|decision-mapping" -- .omc/drafts/issue-tracker-ark-additions.md`
Expected: no output.

- [ ] **Step 4: Commit**

```bash
git add .omc/drafts/issue-tracker-ark-additions.md
git commit -m "docs: sweep retired /to-issues ref in stale issue-tracker draft"
```

---

## Task 6: `docs/agents/issue-tracker.md` — rename + research-landing cross-ref

Phase 1a rename (L31/L48/L72) plus the Phase 3 cross-reference line pointing at the CLAUDE.md research row (added in Task 7).

**Ordering:** this task adds a cross-ref to the `CLAUDE.md` **Research notes** row that Task 7 creates. To avoid a transient dangling reference, **execute Task 7 before this task** (the target row already exists when the cross-ref lands).

**Files:**
- Modify: `docs/agents/issue-tracker.md` (L31, L48, L72, + new cross-ref line)

**Interfaces:**
- Consumes: the CLAUDE.md `Research notes` row (Task 7) — this file points at it.
- Produces: nothing.

- [ ] **Step 1: Edit line 31**

Change `(`to-issues`, `triage`, `to-prd`, `qa`)` to `(`to-tickets`, `triage`, `to-spec`, `code-review`)`. (Drops retired `qa`; adds `code-review`.)

- [ ] **Step 2: Edit line 48**

Change `any comments posted by mattpocock's `/to-issues` or` to `any comments posted by mattpocock's `/to-tickets` or`.

- [ ] **Step 3: Edit line 72**

Change `child-cutting to mattpocock `/to-issues` per the validated contract in` to `child-cutting to mattpocock `/to-tickets` per the validated contract in`.

- [ ] **Step 4: Add the research-landing cross-ref**

At the end of the "### ark-skills additions" section (after the Dual-write rule block), add a short subsection:

```markdown
### Research output landing

mattpocock's `/research` skill writes cited-Markdown findings to the path declared in
`CLAUDE.md` → Project Configuration → **Research notes** (`vault/Research/`). `/vault`
then distills those files into OKF-conformant pages. See `skills/vault/SKILL.md` §2.
```

- [ ] **Step 5: Verify**

Run: `git grep -nE "to-issues|to-prd|to-plan|decision-mapping" -- docs/agents/issue-tracker.md`
Expected: no output.
Run: `git grep -n "Research notes" docs/agents/issue-tracker.md`
Expected: the new cross-ref line.

- [ ] **Step 6: Commit**

```bash
git add docs/agents/issue-tracker.md
git commit -m "docs(issue-tracker): rename to v1.1.0 + research-output landing cross-ref"
```

---

## Task 7: Phase 3 research → vault wire-up (CLAUDE.md + vault SKILL)

Register the discoverable landing path where `/research` actually resolves it (CLAUDE.md), and name `research` output as a first-class `/vault` ingest source.

**Files:**
- Modify: `CLAUDE.md:66` (add a table row after Task Management)
- Modify: `skills/vault/SKILL.md:53-64` (§2 Document ingestion)

**Interfaces:**
- Consumes: nothing.
- Produces: the `Research notes → vault/Research/` config row that `issue-tracker.md` (Task 6) and `/research` resolve against.

- [ ] **Step 1: Add the CLAUDE.md Project Configuration row**

After line 66 (the `**Task Management**` row) in the `## Project Configuration` table, add:
```
| **Research notes** | `vault/Research/` — landing path for `/research` cited-Markdown output, then distilled by `/vault` |
```

- [ ] **Step 2: Add the research-ingest note to `skills/vault/SKILL.md` §2**

The `### 2 — Document ingestion` section ends with `4. Finish with the index + lint step (below).`, immediately followed by the `### 3 — log.md append (work-record mirror)` heading. Insert the new paragraph between them. Replace this exact block:
```markdown
4. Finish with the index + lint step (below).

### 3 — log.md append (work-record mirror)
```
with:
```markdown
4. Finish with the index + lint step (below).

**`/research` output is a first-class ingest source.** mattpocock's `/research` skill
writes cited-Markdown findings to `vault/Research/` (declared in `CLAUDE.md` → Project
Configuration → Research notes). Ingesting them is the same distillation as above:
give each an OKF `type:`, a ≤200-char `description:`, and populate `source-tasks:` with
the originating GitHub issue number(s). Distill — do not paste the raw research file.

### 3 — log.md append (work-record mirror)
```

Note: use `description:` (not `summary:`) — that is the field the vault skill's step 2
already names and `vault/_meta/okf/okf_lint.py` enforces.

- [ ] **Step 3: Verify both edits**

Run: `git grep -n "Research notes" CLAUDE.md`
Expected: the new row.
Run: `git grep -n "first-class ingest source" skills/vault/SKILL.md`
Expected: the new note.

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md skills/vault/SKILL.md
git commit -m "feat(vault): wire mattpocock /research output into OKF ingestion (vault/Research/)"
```

---

## Task 8: Phase 4 version bump 2.2.1 → 2.3.0

New convergence behavior → minor bump. Four surfaces plus changelog.

**Files:**
- Modify: `VERSION`
- Modify: `.claude-plugin/plugin.json:3`
- Modify: `.claude-plugin/marketplace.json:12`
- Modify: `CHANGELOG.md` (new top entry)

**Interfaces:**
- Consumes / Produces: nothing.

- [ ] **Step 1: `VERSION`**

Replace the single line `2.2.1` with `2.3.0`.

- [ ] **Step 2: `.claude-plugin/plugin.json` line 3**

Change `"version": "2.2.1",` to `"version": "2.3.0",`.

- [ ] **Step 3: `.claude-plugin/marketplace.json` line 12**

Change `"version": "2.2.1"` to `"version": "2.3.0"`.

- [ ] **Step 4: `CHANGELOG.md` — insert new top entry**

Immediately after the `All notable changes...` line and before `## [2.2.1] - 2026-07-07`, insert:
```markdown
## [2.3.0] - 2026-07-08

Converge ark-skills onto **mattpocock/skills v1.1.0**. v1.1.0 renamed the skills
`/ark-consult` routes to and `/ark-health` checks for; ark-skills still referenced the
retired names, which broke Check 3 and dangled the delegation contract.

### Fixed

- **Retired-name drift** — replaced `to-issues`/`to-prd`/`to-plan` refs with
  `to-tickets`/`to-spec` across `ark-consult`, `ark-health`, `ark-onboard`,
  `issue-tracker.md`, `README`, `AGENTS.md`, `skills/AGENTS.md`, and the tracked
  `.omc/drafts/` contract + additions drafts.
- **`/ark-health` Check 3** — hardened to require `to-tickets` + `setup-matt-pocock-skills`
  plus ≥2 of `to-spec`/`triage`/`implement`, fixing a false-fail on upgraded installs
  and a latent false-pass when `to-tickets` is missing.

### Changed

- **`/ark-consult`** — the mattpocock lane now delegates child-cutting to `/to-tickets`
  (native blocking edges + sub-issues + expand–contract for wide refactors); entry
  points sharpened (`/to-spec`→`/to-tickets` for held-in-head builds, `/implement` for
  filed issues). Foggy multi-session work routes to the OMC lane; `/wayfinder` stays a
  manual off-profile choice under the Minimal label model (no `wayfinder:*` labels
  adopted).
- **`.omc/drafts/mattpocock-contract.md`** — rewritten against the installed v1.1.0
  skills (`to-tickets`/`to-spec`/`triage`/`code-review`).

### Added

- **`/research` → vault ingestion** — `/research` cited-Markdown output lands in
  `vault/Research/` (declared in `CLAUDE.md`) and is a first-class `/vault` ingest
  source, so the mattpocock lane produces durable OKF knowledge.
```

- [ ] **Step 5: Verify version sync**

Run: `grep -RnE "2\.3\.0" VERSION .claude-plugin/plugin.json .claude-plugin/marketplace.json CHANGELOG.md`
Expected: one hit in each of the four files.
Run: `grep -RnE "2\.2\.1" VERSION .claude-plugin/plugin.json .claude-plugin/marketplace.json`
Expected: no output (2.2.1 remains only in the historical CHANGELOG entry).

- [ ] **Step 6: Commit**

```bash
git add VERSION .claude-plugin/plugin.json .claude-plugin/marketplace.json CHANGELOG.md
git commit -m "chore(release): bump 2.2.1 → 2.3.0 (mattpocock v1.1.0 convergence)"
```

---

## Task 9: Whole-repo verification sweep

Final audit that no retired mattpocock name survives and every `review`/`qa` left is a legitimate non-mattpocock use.

**Files:** none (verification only).

- [ ] **Step 1: Unambiguous-name sweep**

Run (sweeps ALL tracked files minus the three historical records that legitimately name the old skills — the `vault/**` history, the `docs/superpowers/**` spec+plan, and `CHANGELOG.md`):
```bash
git grep -nE "to-issues|to-prd|to-plan|decision-mapping" -- ':!vault/**' ':!docs/superpowers/**' ':!CHANGELOG.md'
```
Expected: no output. (These three paths record verbatim history / the rename map itself; every *live* reference must be clean.)

- [ ] **Step 2: Classification audit for `review`/`qa`**

Run:
```bash
git grep -nE 'mattpocock.{0,80}/(review|qa)|/(review|qa).{0,80}mattpocock' -- ':!vault/**' ':!docs/superpowers/**'
```
Expected: no mattpocock-context hits. Manually confirm any `review`/`qa` still present (e.g. `ark-health:43`, `ark-consult:49`) are gstack-detection or generic prose, per the classification rule.

- [ ] **Step 3: Contract cross-reference integrity**

Run: `git grep -n "mattpocock-contract.md" -- skills/ docs/`
Expected: the `ark-consult` and `issue-tracker.md` pointers still resolve to the (rewritten, still-present) file.

- [ ] **Step 4: Re-run hardened Check 3**

Run the Task 2 Step 5 snippet.
Expected: `PASS: to-tickets+setup present, 3/3 optional`.

- [ ] **Step 5: Version sync**

Run: `grep -RnE "2\.3\.0" VERSION .claude-plugin/plugin.json .claude-plugin/marketplace.json`
Expected: one hit each.

- [ ] **Step 6: No commit** — verification only. If any check fails, return to the owning task.

---

## Self-Review (completed by plan author)

- **Spec coverage:** Phase 1 → Tasks 1–6; Phase 2 → Task 3 (+ contract in Task 4); Phase 3 → Tasks 6–7; Phase 4 → Task 8; whole-spec verification → Task 9. The stale-draft flag → Task 5. Every spec section maps to a task.
- **Placeholder scan:** no TBD/TODO; every edit shows exact before/after text or full replacement content.
- **Type/name consistency:** the rename map is applied identically everywhere; `to-tickets`/`to-spec`/`code-review`/`wayfinder` spelled consistently across tasks; Check 3 roster in Task 2 matches the verification in Task 9.
- **Classification safety:** the two generic-word sites (`ark-health:43`, `ark-consult:49`) are explicitly protected in Tasks 2 and 3 and re-audited in Task 9.
```
