---
tags:
  - task
title: "/ark-context-warmup — Automatic Context Loader"
task-id: "Arkskill-002"
status: done
priority: high
project: "ark-skills"
work-type: development
task-type: epic
urgency: normal
session: "S006"
created: "2026-04-12"
last-updated: "2026-04-13"
---

# Arkskill-002: /ark-context-warmup — Automatic Context Loader

## Summary

Add an automatic context loader that runs as step 0 of every `/ark-workflow` chain. Queries `/notebooklm-vault`, `/wiki-query`, and `/ark-tasknotes` backends in a partial-parallel fan-out, synthesizes a single Context Brief, and surfaces possible duplicates / prior rejections / in-flight collisions as Evidence candidates. Cache keyed on `chain_id + task_hash`, 2-hour TTL, 24-hour pruning.

## Spec & Plan

- Design: `docs/superpowers/specs/2026-04-12-ark-context-warmup-design.md`
- Implementation plan: `docs/superpowers/plans/2026-04-12-ark-context-warmup-implementation.md` (21 tasks, all DONE)

## Implementation — DONE (v1.12.0, PR #14)

### Core (Tasks 1-7)

- [x] `scripts/warmup-helpers.py` — task_normalize, task_summary, task_hash, chain_id_new (ULID)
- [x] `scripts/contract.py` — warmup_contract YAML parser + validator + relative-script-path resolution
- [x] `scripts/availability.py` — per-backend probes (NotebookLM, wiki, tasknotes)
- [x] `scripts/evidence.py` — deterministic evidence candidate generator (spec D3)
- [x] `scripts/synthesize.py` — atomic cache writes + 24h pruning + YAML-safe frontmatter
- [x] `scripts/executor.py` — input resolution, precondition, shell substitution, JSONPath extraction

### Backend contracts (Tasks 8-11)

- [x] `skills/notebooklm-vault/SKILL.md` — `warmup_contract` block with session-continue + bootstrap commands
- [x] `skills/notebooklm-vault/scripts/session_shape_check.sh` — precondition for session-continue
- [x] `skills/wiki-query/SKILL.md` — warmup_contract with per-scenario templates
- [x] `skills/wiki-query/scripts/warmup_scan.py` — T4-only scan, handles bullet + table-form index
- [x] `skills/ark-tasknotes/SKILL.md` — warmup_contract with status + search
- [x] `skills/ark-tasknotes/scripts/warmup_search.py` — task search, spec-D3 component extraction

### Orchestration (Tasks 12-17)

- [x] `skills/ark-workflow/SKILL.md` Step 6.5 — extended chain-file frontmatter (chain_id, task_text, task_summary, task_normalized, task_hash)
- [x] All 7 chain files — `/ark-context-warmup` as step 0
- [x] `skills/ark-context-warmup/SKILL.md` — main orchestration skill
- [x] `scripts/check_chain_integrity.py` — CI check for step-0 insertion + handoff markers
- [x] `scripts/check_contract_extension.py` — CI check for Step 6.5 frontmatter fields
- [x] `scripts/smoke-test.md` — manual runbook (9 cases, drill pending — see S006 Next Steps)
- [x] Bats integration tests (committed, bats-core run deferred)

### Fixtures + Registration (Tasks 18-21)

- [x] 9 evidence-candidate regression fixtures replayed via `test_fixtures.py`
- [x] Step 0 inserted in every chain's every weight-class section; handoff markers preserved
- [x] Chain-numbering behavior pinned (plan's Task 20 note — chain numbering kept original)
- [x] `/ark-context-warmup` registered in README + CLAUDE.md Available Skills list

## Post-Implementation Hardening (codex P1/P2 fixes)

- [x] YAML-safe frontmatter (chain + cache brief) — block scalar `|-`
- [x] `json_path_template` on both `notebook_id` specs (was KeyError)
- [x] Template env var interpolation + iterative fixed-point (wiki two-layer indirection)
- [x] Empty required fields accepted as valid output (not demoted to Degraded coverage)
- [x] Wiki lane availability only requires `index.md`
- [x] Python 3.9 compatibility via future annotations (except executor.py dataclass)
- [x] Precondition script paths resolved against backend skill dir
- [x] `shlex.quote` on substituted shell values; 3 backend `shell:` templates drop surrounding quotes
- [x] `_interpolate_template` iterates until fixed-point
- [x] `warmup_scan` handles table-form `index.md` generated by `_meta/generate-index.py`
- [x] TaskNotes lane keys off `Tasks/` dir (not counter file); component extraction per spec D3
- [x] `backlog` in active-status set for duplicate detection

## Tests

107 → 143 (+36 regression tests from codex findings). See S006 for the commit-by-commit arc.

## Next Steps (post-ship, see S006)

- Refactor `synthesize.assemble_brief` to accept structured NotebookLM output (separate fields for session-continue vs bootstrap shapes)
- `/ark-context-warmup` Step 4 — export `WARMUP_SCENARIO_QUERY_TEMPLATE` before dispatching wiki lane (prose documented, no test)
- Bats integration test execution
- Smoke-test runbook drill against real NotebookLM + vault
